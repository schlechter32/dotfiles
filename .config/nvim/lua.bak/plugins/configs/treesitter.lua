local options = {
  -- ensure_installed = { "lua" },
ensure_installed = {  "javascript", "typescript", "c", "lua", "rust" ,"norg"}, 
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
